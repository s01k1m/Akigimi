{
  // npm install eslint-config-naver  --save-dev
  // npm install prettier --save-dev --save-exact
  "extends": ["next/core-web-vitals", "naver", "prettier"],
  "rules": {
    // "no-console": "off" //  1로 주면 경고
  }
}
